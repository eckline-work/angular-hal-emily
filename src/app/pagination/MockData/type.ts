export class errorType {

  code: string;
  desc: string;
  email: string;
  pager: string;

  constructor(
    code: string,
    desc: string,
    email: string,
    pager: string,
    ) { }
    
}